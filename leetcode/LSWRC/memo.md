# イテレータ
配列やコンテナの中身を指し示す、Cでいうポインタのようなもの。というかほぼポインタ。
```c++
vector<int> v = {10,20,30};
```
このときに
v[0] = 10
v[1] = 20
v[2] = 30
コイツラを指し示すため
```
auto it = v.begin;
```
という、イテレータを使用する。
ほぼポインタなので、
```
cout << *it << endl;
```
とすると
```sh
10
```
が出力される。